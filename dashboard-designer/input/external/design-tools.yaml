# 外部设计工具配置
# 请根据实际情况填写配置信息

tools:
  # Figma配置
  - name: Figma
    vendor: Figma Inc.
    type: design
    enabled: false
    auth:
      type: token
      token: ${FIGMA_TOKEN}
    projects: []

  # 墨刀配置
  - name: 墨刀
    vendor: Modao.cc
    type: prototype
    enabled: false
    auth:
      type: token
      token: ${MODAO_TOKEN}
    projects: []
